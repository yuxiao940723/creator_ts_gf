import ShaderBase from "../base/ShaderBase";

const {ccclass, property} = cc._decorator;

@ccclass
export default class BallBase extends ShaderBase {

    path = "ball/";

    start () {
        
    }

    // update (dt) {}
}